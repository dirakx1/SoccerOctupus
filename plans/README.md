# Implementation Plans

Generated by the improve skill on 2026-06-19 and updated on 2026-06-26. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Implement Stripe subscriptions and billing self-service | P1 | L | — | DONE - verified 2026-06-23 against configured test Supabase database |
| 002 | Implement cycle-based feature usage limits | P1 | L | 001 | DONE - verified 2026-06-24 |
| 003 | Harden Stripe webhook subscription sync | P1 | M | 001, 002 | DONE - verified 2026-06-25 |
| 004 | Add production-ready failed-payment recovery | P1 | M | 001, 003 | DONE - verified 2026-06-26 |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plan 001 has no plan dependencies. It should be executed as one coordinated feature because schema, webhook sync, entitlement checks, and frontend billing flows depend on the same subscription contract.
- Plan 002 depends on Plan 001 because it uses the Stripe subscription tier/status fields, billing routes, and frontend profile billing section introduced by the subscription work. Free tier remains off Stripe; Stripe only supplies paid subscription state and paid period dates.
- Plan 003 depends on Plans 001 and 002 because it hardens the Stripe webhook and subscription sync paths added by Plan 001 and preserves the subscription period fields used by cycle-based limits from Plan 002.
- Plan 004 depends on Plans 001 and 003 because it builds on the Stripe subscription fields, Customer Portal helper, and refetch-before-sync webhook behavior. It intentionally does not depend on Plan 002 except for preserving existing usage-limit behavior.

## Findings considered and rejected

- Full repo audit findings: not run for this invocation because the request was a specific Stripe subscriptions implementation plan, so this used the improve skill's `plan <description>` path rather than a broad audit.
- Free tier in Stripe: rejected for Plan 002. It would add `$0` subscription lifecycle and portal cancellation edge cases. The chosen design keeps Free usage cycles in the app database while paid tiers remain in Stripe.
- Broad Stripe lifecycle expansion: rejected for Plan 003 per the explicit request to handle `invoice.paid`, fetch current Stripe resources before syncing, add/verify deduplication, and leave the rest.
- Custom failed-payment emails: rejected for Plan 004. Stripe Billing already owns dunning emails and retries; the app should first expose billing-health state and direct users to Stripe's payment-method update flow.
- New failed-payment database table: rejected for Plan 004. Existing `users.subscription_status`, period fields, and Stripe webhook sync are enough for a production-ready recovery UX; durable payment-event history can be planned later if support/audit requirements demand it.
